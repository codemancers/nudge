<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8"/>
    <meta http-equiv="X-UA-Compatible" content="IE=edge"/>
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    <title>Nudge · Without bells and whistles</title>
    <link rel="stylesheet" href="<%= Routes.static_path(@conn, "/css/app.css") %>"/>
    <script defer type="text/javascript" src="<%= Routes.static_path(@conn, "/js/app.js") %>"></script>
  </head>
  <body>
    <header>
      <section class="container">
        <nav role="navigation">
          <ul>
            <%= if assigns[:current_user] do %>
              <li><%= link "Logout", to: Routes.session_path(@conn, :logout), class: "navbar-item" %></li>
            <% else %>
            <li><%= link "Login", to: Routes.session_path(@conn, :login), class: "navbar-item" %></li>
            <li><%= link "Signup", to: Routes.session_path(@conn, :signup), class: "navbar-item" %></li>
            <% end %>
          </ul>
        </nav>
        <a href="/" class="phx-logo">
          <h3>Nudge</h3>
        </a>
      </section>
    </header>
    <main role="main" class="container">
      <p class="alert alert-info" role="alert"><%= get_flash(@conn, :info) %></p>
      <p class="alert alert-danger" role="alert"><%= get_flash(@conn, :error) %></p>
      <%= @inner_content %>
    </main>
  </body>
</html>
